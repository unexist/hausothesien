const targetDate=new Date('2026-07-04T14:00:00');const countdownElement=document.getElementById('countdown');function updateCountdown(){if(!countdownElement)return;const now=new Date();const diff=targetDate-now;if(diff<=0){countdownElement.innerHTML='🎉 HAUSOTHESIEN LÄUFT JETZT';return}const days=Math.floor(diff/(1000*60*60*24));const hours=Math.floor((diff%(1000*60*60*24))/(1000*60*60));const minutes=Math.floor((diff%(1000*60*60))/(1000*60));countdownElement.innerHTML=`${days} Tage · ${hours} Stunden · ${minutes} Minuten`}updateCountdown();setInterval(updateCountdown,1000);
